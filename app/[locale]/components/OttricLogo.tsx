export function OttricLogo({ className }: { className?: string }) {
  return (
    <span className={`${className} font-bold`}>
      <span className="font-(family-name:--font-geist-sans) font-black">O</span>ttric
    </span>
  );
}
